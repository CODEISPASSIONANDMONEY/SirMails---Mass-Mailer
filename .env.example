# Email configuration
# For Gmail: Use your Gmail address and an App Password (not your regular password)
# To create an App Password: https://myaccount.google.com/apppasswords
# IMPORTANT: Enable 2-Step Verification first, then generate App Password

EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-character-app-password

# SMTP Configuration (optional - only if using custom SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Server Configuration
PORT=3000
HOST=0.0.0.0
